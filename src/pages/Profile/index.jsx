import React from 'react'

import { Tasks, Timer } from '../../components'

const Profile = () => {
    return (
        <div>
            <Tasks/>
            <Timer/>
        </div>
    )
}

export default Profile
